import React from 'react';
import DefaultLayout from '../../layout/DefaultLayout';

const CoordinatorDistribution = () => {
    return (
        <DefaultLayout>
        <div>
            <h1>Coordinator Distibution page</h1>
        </div>
        </DefaultLayout>
    );
};

export default CoordinatorDistribution;