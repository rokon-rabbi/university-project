import React from 'react';
import DefaultLayout from '../../layout/DefaultLayout';

const CoordinatorClasses = () => {
    return (
        <DefaultLayout>
        <div>
            <h1>Coordinator classes page</h1>
        </div>
        </DefaultLayout>
    );
};

export default CoordinatorClasses;