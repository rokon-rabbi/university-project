import React from 'react';
import DefaultLayout from '../../layout/DefaultLayout';

const ExamEntry = () => {
    return (
        <DefaultLayout>
        <div>
            <h1>Exam entry page</h1>
        </div>
        </DefaultLayout>
    );
};

export default ExamEntry;